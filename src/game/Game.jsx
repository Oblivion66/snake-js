import React from 'react';
import Canvas from '../canvas/Canvas';
import useCanvas from '../canvas/useCanvas';

const Game = () => {
    return (
        <div>
            
        </div>
    );
};

export default Game;